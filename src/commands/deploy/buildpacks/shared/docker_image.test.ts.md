# Snapshot report for `src/commands/deploy/buildpacks/shared/docker_image.test.ts`

The actual snapshot is saved in `docker_image.test.ts.snap`.

Generated by [AVA](https://avajs.dev).

## node dockerfile: default npm start

> Snapshot 1

    `FROM node:22.1.0-slim␊
    LABEL com.faable.cloud="FaableCloud"␊
    LABEL description="Faablecloud automatic deployment"␊
    ␊
    WORKDIR /faable/app␊
    ␊
    # Environment variables for runtime␊
    ENV PORT=80␊
    ENV NODE_ENV=production␊
    # \`escape\` keeps \`$PORT\` literal so the build heredoc (unquoted bash) doesn't␊
    # eat it; Docker then expands $PORT (=80) when building the ENV value.␊
    ENV START_COMMAND="npm run start"␊
    ␊
    ␊
    # Copy Usercode␊
    COPY . .␊
    ␊
    ␊
    # Entrypoint stript␊
    RUN echo '#!/bin/sh\\n\\nNODE_VERSION=\\$(node --version)\\nNPM_VERSION=\\$(npm --version)\\nYARN_VERSION=\\$(yarn --version)\\n\\necho "Faable Cloud · [node \\$NODE_VERSION] [npm \\$NPM_VERSION] [yarn \\$YARN_VERSION]"\\neval \\$START_COMMAND\\n' >> entrypoint.sh␊
    ␊
    CMD ["/bin/sh", "./entrypoint.sh"]␊
    `

## node dockerfile: static framework serve command keeps $PORT escaped

> Snapshot 1

    `FROM node:20.11.1-slim␊
    LABEL com.faable.cloud="FaableCloud"␊
    LABEL description="Faablecloud automatic deployment"␊
    ␊
    WORKDIR /faable/app␊
    ␊
    # Environment variables for runtime␊
    ENV PORT=80␊
    ENV NODE_ENV=production␊
    # \`escape\` keeps \`$PORT\` literal so the build heredoc (unquoted bash) doesn't␊
    # eat it; Docker then expands $PORT (=80) when building the ENV value.␊
    ENV START_COMMAND="npx serve -s build -l \\$PORT"␊
    ␊
    ␊
    # Copy Usercode␊
    COPY . .␊
    ␊
    ␊
    # Entrypoint stript␊
    RUN echo '#!/bin/sh\\n\\nNODE_VERSION=\\$(node --version)\\nNPM_VERSION=\\$(npm --version)\\nYARN_VERSION=\\$(yarn --version)\\n\\necho "Faable Cloud · [node \\$NODE_VERSION] [npm \\$NPM_VERSION] [yarn \\$YARN_VERSION]"\\neval \\$START_COMMAND\\n' >> entrypoint.sh␊
    ␊
    CMD ["/bin/sh", "./entrypoint.sh"]␊
    `

## python dockerfile: cacheable requirements layer before COPY . .

> Snapshot 1

    `FROM python:3.11.3-slim␊
    LABEL com.faable.cloud="FaableCloud"␊
    LABEL description="Faablecloud automatic deployment"␊
    ␊
    WORKDIR /faable/app␊
    ␊
    # Environment variables for runtime␊
    ENV PORT=80␊
    ENV PYTHONUNBUFFERED=1␊
    # \`escape\` keeps \`$PORT\` literal so the build heredoc (unquoted bash) doesn't␊
    # eat it; Docker then expands $PORT (=80) when building the ENV value.␊
    ENV START_COMMAND="uvicorn main:app --host 0.0.0.0 --port \\$PORT"␊
    ␊
    # Copy dependency manifests first so the install layer stays cached until␊
    # they change (any source change no longer reinstalls dependencies).␊
    COPY requirements.txt ./␊
    RUN pip install --no-cache-dir -r requirements.txt && pip install --no-cache-dir uvicorn[standard]␊
    ␊
    # Copy Usercode␊
    COPY . .␊
    ␊
    ␊
    # Entrypoint stript␊
    RUN echo '#!/bin/sh\\n\\nPYTHON_VERSION=\\$(python --version 2>&1)\\nPIP_VERSION=\\$(pip --version 2>&1)\\n\\necho "Faable Cloud · [\\$PYTHON_VERSION] [\\$PIP_VERSION]"\\neval \\$START_COMMAND\\n' >> entrypoint.sh␊
    ␊
    CMD ["/bin/sh", "./entrypoint.sh"]␊
    `

## python dockerfile: source-dependent install runs after COPY . .

> Snapshot 1

    `FROM python:3.12-slim␊
    LABEL com.faable.cloud="FaableCloud"␊
    LABEL description="Faablecloud automatic deployment"␊
    ␊
    WORKDIR /faable/app␊
    ␊
    # Environment variables for runtime␊
    ENV PORT=80␊
    ENV PYTHONUNBUFFERED=1␊
    # \`escape\` keeps \`$PORT\` literal so the build heredoc (unquoted bash) doesn't␊
    # eat it; Docker then expands $PORT (=80) when building the ENV value.␊
    ENV START_COMMAND="gunicorn app:app --bind 0.0.0.0:\\$PORT"␊
    ␊
    ␊
    # Copy Usercode␊
    COPY . .␊
    ␊
    # Install needs the full source (e.g. \`pip install .\`), so it runs after COPY.␊
    RUN pip install --no-cache-dir .␊
    ␊
    # Entrypoint stript␊
    RUN echo '#!/bin/sh\\n\\nPYTHON_VERSION=\\$(python --version 2>&1)\\nPIP_VERSION=\\$(pip --version 2>&1)\\n\\necho "Faable Cloud · [\\$PYTHON_VERSION] [\\$PIP_VERSION]"\\neval \\$START_COMMAND\\n' >> entrypoint.sh␊
    ␊
    CMD ["/bin/sh", "./entrypoint.sh"]␊
    `

## escape survives quotes, backticks, dollars and newlines

> Snapshot 1

    `FROM python:3.12-slim␊
    LABEL com.faable.cloud="FaableCloud"␊
    LABEL description="Faablecloud automatic deployment"␊
    ␊
    WORKDIR /faable/app␊
    ␊
    # Environment variables for runtime␊
    ENV PORT=80␊
    # \`escape\` keeps \`$PORT\` literal so the build heredoc (unquoted bash) doesn't␊
    # eat it; Docker then expands $PORT (=80) when building the ENV value.␊
    ENV START_COMMAND="sh -c \\'echo "hi \\\`whoami\\\`" && python -m app --port \\$PORT\\'"␊
    ␊
    ␊
    # Copy Usercode␊
    COPY . .␊
    ␊
    # Install needs the full source (e.g. \`pip install .\`), so it runs after COPY.␊
    RUN true␊
    ␊
    # Entrypoint stript␊
    RUN echo '#!/bin/sh\\n\\nPYTHON_VERSION=\\$(python --version 2>&1)\\nPIP_VERSION=\\$(pip --version 2>&1)\\n\\necho "Faable Cloud · [\\$PYTHON_VERSION] [\\$PIP_VERSION]"\\neval \\$START_COMMAND\\n' >> entrypoint.sh␊
    ␊
    CMD ["/bin/sh", "./entrypoint.sh"]␊
    `
